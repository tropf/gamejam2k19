[gd_scene load_steps=6 format=2]

[ext_resource path="res://assets/graphics/ball.png" type="Texture" id=1]

[sub_resource type="GDScript" id=3]
script/source = "extends Node2D

# Called when the node enters the scene tree for the first time.
func _ready():
	# add ball node to global list
	Global.gravityobjects.append(self)

	
	pass 

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):

	pass
"

[sub_resource type="GDScript" id=1]
script/source = "extends Sprite

var rng = RandomNumberGenerator.new()

export var color = Color(0,0,0)

export var rotationspeed = 360

# Called when the node enters the scene tree for the first time.
func _ready():
	get_parent().set_modulate(color)
	pass 
	

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):

	
	get_parent().rotation_degrees += rotationspeed*delta
	pass"

[sub_resource type="GDScript" id=4]
script/source = "extends RigidBody2D

# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	# Reset force
	
	applied_force = Vector2(0, 0)
	
	# Apply each force
	for i in Global.gravityobjects:
		applied_force = (Global.gravity * (mass * i.get_node(\"RigidBody2D\").get_mass()) * delta)
		print(\"\")
	pass
"

[sub_resource type="CircleShape2D" id=2]
radius = 212.79

[node name="Ball" type="Node2D"]
script = SubResource( 3 )

[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )
script = SubResource( 1 )
color = Color( 0.909804, 0.0509804, 0.0509804, 1 )

[node name="RigidBody2D" type="RigidBody2D" parent="."]
mass = 100.0
script = SubResource( 4 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="RigidBody2D"]
visible = false
shape = SubResource( 2 )
